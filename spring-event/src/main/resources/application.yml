spring:
  datasource:
    url: jdbc:mysql://localhost:8080/myapp?serverTimezone=UTC&characterEncoding=UTF-8
    username: haneul
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
